<?xml version="1.0" encoding="UTF-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:shared="clr-namespace:LibVLCSharp.Forms.Shared;assembly=LibVLCSharp.Forms"
             x:Class="Xamarin.Forms.TikTok.Controls.HomeViewVideoControl"
             x:Name="Root">
    <Grid>
        <shared:VideoView MediaPlayer="{Binding Source={x:Reference Root}, Path=MediaPlayer}"/>
        
        <Grid BackgroundColor="Black"
              Margin="0,0,0,-10"
              IsClippedToBounds="False"
              VerticalOptions="End">
              <Grid HeightRequest="15"/>
        </Grid>

        <Slider ThumbColor="White"
                Maximum="1.0"
                Value="{Binding Source={x:Reference Root}, Path=Position}"
                IsVisible="{Binding Source={x:Reference Root}, Path=PositionVisible}"
                VerticalOptions="End"
                Margin="-14, 0, -14, -2"
                InputTransparent="True"
                MaximumTrackColor="Gray"
                MinimumTrackColor="White"/>
    </Grid>
</ContentView>